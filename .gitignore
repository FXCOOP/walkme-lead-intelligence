.env
credentials.json
__pycache__/
*.pyc
.DS_Store

# Keep canonical Python output in the repo — the /demo endpoint reads it.
# output/ is NOT ignored anymore.

# Web UI (Next.js on Vercel) is a separate deploy target; exclude its local
# node_modules and build artifacts from the Python deploy to keep the bundle small.
web/node_modules/
web/.next/
web/.vercel/
